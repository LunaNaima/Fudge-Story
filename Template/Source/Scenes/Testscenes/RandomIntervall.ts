namespace Spiegel_VN {
  export async function RandomIntervall(): ƒS.SceneReturn {
    // function getRandomInt(min, max) {
    //   min = Math.ceil(min);
    //   max = Math.floor(max);
    //   return Math.floor(Math.random() * (max - min + 1)) + min;
    // }
    // let randomTextChp01Marketplace = ƒ.Random.default.(1, 5);
    // let setRandomInterval = (intervalFunction: number, minDelay: number, maxDelay: number) => {
    //     let timeout;
    //     const runInterval = () => {
    //       const timeoutFunction = () => {
    //         intervalFunction();
    //         runInterval();
    //       };
    //       const delay = Math.floor(Math.random() * (maxDelay - minDelay + 1)) + minDelay;
    //       timeout = setTimeout(timeoutFunction, delay);
    //     };
    //     runInterval();
    //     return {
    //       clear() { clearTimeout(timeout) },
    //     };
    //   };
    // ƒ.EventTimer.arguments(1, 5)
  }
}
